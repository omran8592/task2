on:
  push:
    branches:
      - master




jobs:
  build:
    runs-on: ubuntu-lastest

    steps:
      - name: 'Before'
        run: ls
      - uses: actions/checkout@v3
      
      - name: 'After'
        run: ls